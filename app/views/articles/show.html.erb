

  <blockquote> 
  <b> <%= @article.user.name %> </b>
  <time> <%= l @article.updated_at, format: :short %> </time>
  <h2> <%= @article.name %></h2> 
  </blockquote>
  
  <div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-8">   
      <p> <%= @article.body.html_safe %> </p>
      <hr>
      <div id="newComment">
        <%=  render 'new_comment' %> 
      </div>
      
      <div id="comments">
        <%=  render 'comments' %>  
      </div>
    </div>
    <div class="col-md-3"></div>
  </div>



<button class="btn btn-info btn-small center" id="more"> Załaduj więcej!</button>

<script type="text/javascript">


  $('body').css('background-image', 'url(' + '<%= @blog.avatar.url %>'  + ')' );
  







  var counter = 2;
  removeButon(counter,<%= @article.comments.count %>, '#more');
  var url = '/blogs/'+ <%= @blog.id %> +'/articles/'+<%= @article.id %> +'?page='+counter+'';
  var divv = '#comments';
  $('#more').on('click',function(){
    counter = myPagination(url,counter,divv, '#more' );
    removeButon(counter,<%= @article.comments.count %>, '#more');
    url = '/blogs/'+ <%= @blog.id %> +'/articles/'+<%= @article.id %> +'?page='+counter+'';
  });
</script>

